*.iml
.gradle
/local.properties
/.idea
.DS_Store
/captures
.externalNativeBuild
.cxx
local.properties

# Gradle build output — root project and every module
build/

# Firebase config contains project-specific keys — each dev drops in their own
androidApp/google-services.json
iosApp/iosApp/GoogleService-Info.plist

# CocoaPods (iOS) — generated by `pod install`, macOS-only
iosApp/Pods/
iosApp/*.xcworkspace
shared/*.podspec

# Firebase Local Emulator Suite
.firebase/
firebase-debug.log
firestore-debug.log
storage-debug.log
ui-debug.log
# Emulator import/export snapshots — local test data, not source
emulator-data/

# Cloud Functions deps
functions/node_modules/
